// import useCounterStore from '@mf-app/store/counter/store.counter';

// const ReactButton = ({ text }: { text?: string }) => {
//     // const [count, setCount] = useState(0);
//     const { count, actions } = useCounterStore();
//     return (
//         <button className="bg-blue-400 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" onClick={() => actions.inc()}>
//          {text ?? 'count is'} {count}
//         </button>
//     )
// };

// export default ReactButton;